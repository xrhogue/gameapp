import { UniqueRaceDirective } from './unique-race.directive';

describe('UniqueRaceDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueRaceDirective();
    expect(directive).toBeTruthy();
  });
});
