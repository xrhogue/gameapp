import { BetweenDirective } from './between.directive';

describe('BetweenDirective', () => {
  it('should create an instance', () => {
    const directive = new BetweenDirective();
    expect(directive).toBeTruthy();
  });
});
