import { UniqueGenderDirective } from './unique-gender.directive';

describe('UniqueGenderDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueGenderDirective();
    expect(directive).toBeTruthy();
  });
});
